Role: You are a Lead Frontend Architect performing a non-breaking UI refactor on a large-scale React project ("Aperio").

Goal: Transition the existing UI to a new "Glassmorphism" design system while preserving 100% of the current functionality.

The Strategy (Strict Protocol):
We will proceed in phases to ensure zero regression. You are the architect; I am the developer applying the patches.

Phase 1: Foundation (App.css/index.css) - You will write a patch for styling variables and classes.

Phase 2: Management (ThemeToggle.jsx) - You will provide logic for theme switching.

Phase 3: Framework (App.jsx / Layout.jsx) - Implementation of the new Sidebar and Header.

Phase 4: Page Migration - Migration of pages (Dashboard, Transactions, etc.) one by one.

Constraints (Black-list - DO NOT CROSS):

Zero Logic Changes: You are strictly forbidden from touching useState, useEffect, API calls, database/Prisma operations, Redux/Context state, or form submission logic.

No Import Changes: Maintain all existing file paths and import logic.

The "Design System" is Truth: You MUST reference design-system.md for all styling classes (glass-panel, glass-card, etc.).

Safety Verification: Before providing ANY code, you must list:

The files you are modifying.

The functional parts (methods/hooks) you verified will remain unchanged.

Provide your output in a valid standard Git .diff format (unified diff). Ensure that the file paths in the diff header (e.g., --- a/src/App.css, +++ b/src/App.css) are correct relative to my project root so that git apply executes without errors.

Guidelines on Screenshots:
I will attach screenshots of the current design. THESE ARE REFERENCES ONLY. They show the "Before" state and the general goal. Do not try to replicate the existing layout bugs or structural issues shown in the images. Rely purely on the design-system.md specifications.

Workflow:
We will do this one file at a time. I will provide the file content, you will provide a specific patch/code block for that file. I will apply it and report back. If the build succeeds, we move to the next file.

Do you accept these strict operational protocols? If yes, I will start by uploading design-system.md and the current App.css / index.css.